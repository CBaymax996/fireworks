spring.datasource.url=jdbc:sqlite:./fireworks-test.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# 测试环境初始化 schema
fireworks.db.init-schema=true

# tushare 行情数据源（测试用占位值）
tushare.token=test-token
tushare.base-url=https://api.tushare.pro

server.port=8080
